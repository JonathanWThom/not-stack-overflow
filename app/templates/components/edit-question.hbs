{{#if updateQuestionForm}}
  <h6>Edit Question</h6>
  <form>
    <div class="input-field">
      {{input value=author id="author" placeholder=question.author}}
    </div>
    <div class="input-field">
      {{input value=content id="content" placeholder=question.content}}
    </div>
    <div class="input-field">
      {{input value=notes id="notes" placeholder=question.notes}}
    </div>
    <div class="input-field">
      {{input value=tag id="tag" placeholder=question.tag}}
    </div>
    <button class="btn" {{action 'updateQuestion' question}}>Submit Edit</button>
  </form>
{{else}}
  <button class="btn" {{action 'updateQuestionForm'}}>Edit Question</button>
{{/if}}
